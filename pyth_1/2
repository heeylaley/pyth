time = int(input("Введите время в секундах: "))

h = time // 3600
m = (time - h * 3600) // 60
s = time - (h * 3600 + m * 60)

if time > 86399 or time < 0:
    print("Ошибка ввода")
    quit()
else:
    print(f"Сейчас {h:>20}:{m:>5}:{s:>5}")
